server:
  port: 8080
  servlet:
    context-path: /api
spring:
  application:
    name: blog-api
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        show_sql: true